extends ../layout

block content
    .row
        .col-sm-8.offset-sm-2
            .card
                .card-header= 'Edit Source Type: ' + skill_source_type.name
                .card-body
                    form.form.needs-validation(method="POST" action="/skill_source_type/" + skill_source_type.id novalidate)
                        input(type="hidden", name="_method",  value="PUT")
                        input(type="hidden", name='_csrf',value=csrfToken)
                        input(type="hidden", name='skill_source_type[id]',value=skill_source_type.id)
                        include ./_form.pug

append scripts
    script(src='/javascripts/validator.js')
