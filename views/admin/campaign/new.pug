extends ../../layout

include ../../partials/editor

block content
    .row
        .col-sm-10.offset-sm-1
            .card
                .card-header New Campaign
                .card-body
                    form.form.needs-validation(method="POST" action="/admin/campaign" novalidate)
                        input(type="hidden", name='_csrf',value=csrfToken)
                        include ./_form.pug

append scripts
    script(src='/javascripts/validator.js')
    script(src='/javascripts/campaign.js')

