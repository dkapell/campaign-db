extends ../layout

block content
    .row
        .col-sm-8.offset-sm-2
            .card
                .card-header= 'Edit Skill Tag: ' + skill_tag.name
                .card-body
                    form.form.needs-validation(method="POST" action="/skill_tag/" + skill_tag.id novalidate)
                        input(type="hidden", name="_method",  value="PUT")
                        input(type="hidden", name='_csrf',value=csrfToken)
                        input(type="hidden", name='skill_tag[id]',value=skill_tag.id)
                        include ./_form.pug

append scripts
    script(src='/javascripts/validator.js')
    script.
        $(function(){
             $('.select2').select2({
                theme:'bootstrap4',
                minimumResultsForSearch: 6,
                width:'resolve'
            });
        });
